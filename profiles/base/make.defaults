ACCEPT_LICENSE="-* @FREE freedist myspell-ru_RU-ALexanderLebedev"
FEATURES="-config-protect-if-modified"

# System features
USE="alsa bash-completion threads udev unicode -oss"
# Libraries/user features
USE="${USE} cups fftw gmp idn lzma samba sqlite vim-syntax -emacs -ldap"
# Bindings/languages
USE="${USE} -perl -python -mono -java -fortran"
# License issues
USE="${USE} -faac"
# Disable bloatware
USE="${USE} -consolekit -policykit -udisks -upower"
